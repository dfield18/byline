[project]
name = "byline"
version = "0.1.0"
description = "AI search visibility tooling — prompt-running infrastructure"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary,pool]>=3.2",
    "openai>=1.50",
    "google-genai>=1.0",
    "anthropic>=0.40",  # Claude provider (lazy-imported; app runs without it)
    "pyyaml>=6.0",
    "typer>=0.12",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dashboard = [
    "streamlit>=1.30",
    "pandas>=2.0",
]
api = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-jose[cryptography]>=3.3",  # JWT validation (Clerk integration later)
]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
